# 2026-02-21


## Shared memory sync (main request)
- Rewind status: STS/MTS confirmed implemented; LTS scaffolding added locally (uncommitted code) and documented as Rewind-native plan.
- Pushed to GitHub (rewind/rust-native): `docs/rewind-native-imessage-reminders-plan.md` commit `04df1dc`.
- Cron fixes: patched failing isolated cron jobs to model `openai-codex/gpt-5.3-codex`; normalized disk maintenance jobs.
- Disk maintenance: added safe `/data/build/rewind-target` size-cap rotation (default 6GB) to prevent recurrence without hurting dev velocity; post-cleanup `/` ~86-88%, `/data` improved to ~64%.
- Product direction: reminders should be Rewind-native (fork relevant ZeroClaw iMessage/scheduling patterns), with LTS/MTS/STS baked into reminder policy.
- Model availability note: `zai/glm-4.7` currently unusable (subscription over). `nvidia/moonshotai/kimi-k2.5` also not working; prefer Anthropic Sonnet + Google Gemini models as fallbacks.
- iOS app feature idea: investigate `dnakov/litter` ("litter (codex-ios)") — an iOS client for Codex with both remote-only mode and an on-device Rust bridge packaged as an xcframework; consider adapting similar architecture/patterns for our iOS app.
